package series_oop._abstract;

public class _Main_ab {

    // abstract =   abstract classes cannot be instantiated(khởi tạo) but they can have subclass
    //              abstract methods are declared(khai báo) without an implementation(thực hiện)

    public static void main(String[] args) {
    }
}
